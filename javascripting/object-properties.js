//Propiedades del objeto - Object properties
const food = {
    types: 'only pizza'
}

console.log(food.types) //Una manera de llamar al objeto - A way to call the object.
console.log(food['types']) // Otra manera de llamar al objeto - Other way to call the object.