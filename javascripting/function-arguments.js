//Funcion con argumentos - function arguments
function math (a, b, c) {
    let result = (b * c) + a // Operacion matematica - Opetarion matematic
    return result;
}

console.log(math(53, 61, 67)); // Imprimir la funcion y le agregamos parametros - Print the function and add the parameters